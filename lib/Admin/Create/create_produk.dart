import 'package:flutter/material.dart';

class TambahProduk extends StatelessWidget {
  const TambahProduk({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text("ini adalah page create"),
        ),
      ),
    );
  }
}